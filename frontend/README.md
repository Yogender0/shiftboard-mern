# Shift Board - Frontend (React)
## How to run
1. Install dependencies:
   ```bash
   cd frontend
   npm install
   ```
2. Start dev:
   ```bash
   npm start
   ```
3. By default the frontend expects backend at `http://localhost:8080/api`. You can change via `REACT_APP_API_URL`.
